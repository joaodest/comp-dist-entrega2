server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Varios servicos compartilham o dominio localhost (Grafana, Jaeger, etc.) e
  # acumulam cookies grandes que o navegador reenvia para o front. Sem buffers
  # maiores o nginx responde 400 "Request Header Or Cookie Too Large".
  large_client_header_buffers 8 32k;

  location = /frontend-healthz {
    add_header Content-Type text/plain;
    return 200 "ok\n";
  }

  location = /v1/match/ws {
    proxy_pass http://gateway:8080;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
  }

  location /v1/ {
    proxy_pass http://gateway:8080;
    proxy_set_header Host $host;
  }

  location = /healthz {
    proxy_pass http://gateway:8080;
  }

  location = /readyz {
    proxy_pass http://gateway:8080;
  }

  location = /metrics {
    proxy_pass http://gateway:8080;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
